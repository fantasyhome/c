#include<stdio.h>

int main()
{
    int a,b,c;
    a=1;
    b=2*a++;
    a=1;
    c=2*++a;
    printf("%d,%d",b,c);
    return 0;
}
/*
2*a++意味着2*（a++），
意思是递增的结合比乘法高，
递增的是a
而不是（2*a）++
先乘法再将结果递增
至于前缀后缀
那是递增递减的内部规定
2*a++是乘a的原值再递增a
2*++a是a递增后乘2
*/