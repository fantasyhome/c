#include<stdio.h>

int main()
{
    char dont[]={'w','o','w'};
    puts(dont);
    return 0;
}
/*不在末尾加空字符，puts（）会一直打印
内存中的储存数据。直到遇到内存中的空字符
不加'\0'不是字符串，而是字符数组，字符串
会自动把其他位置初始化为空字符，且自动
匹配大小的字符串会自动预留一个空字符的位置
因此不用担心这个问题*/