#include<stdio.h>

int main()
{
    int days[12]={31,28,[4]=31,30,31,[1]=29};
    int i=0;
    for(;i<12;i++)
    printf("%2d %d\n",i+1,days[i]);
    return 0;
}
/*1、数组中有一个元素时，其他元素会初始化为0
2、数组中无元素时其他元素为当前地址的随机值
3、[4]=31,30,31,后面的这些值用于初始化
指定元素后面的元素
4、重复初始化取最后一次的值*/